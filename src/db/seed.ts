/**
 * Strict blank-slate mode:
 * - first launch does not insert categories, transactions, goals, or budgets
 * - only minimal schema + local user row are created by migrations
 * This file is intentionally empty and reserved for explicit opt-in imports.
 */
export const SEED_EMPTY_FOR_REAL_USERS = true;
